//! Mock backend contract.
//!
//! Wave 1 implements this: a fully local fake that returns placeholder media
//! and simulates asynchronous progress, cancellation, and failure — no network,
//! no API keys. It is the only backend the first milestone ships.

// Wave 1 (Track B) fills in `MockBackend` and its `InferenceBackend` impl.
